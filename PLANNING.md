# Wakeup 기획서

## 1. 목적

Wakeup은 Windows와 macOS의 데스크톱 브라우저에서 화면 꺼짐과 잠금을 가능한 동안 방지하는 단일 페이지 웹앱이다. 서버 기능이나 사용자 계정 없이 동작하며, 한 번 온라인으로 불러온 뒤에는 오프라인에서도 다시 실행할 수 있어야 한다.

## 2. 제품 원칙

- 핵심 기능을 가장 먼저, 가장 명확하게 제공한다.
- 기존 구조 안에서 최소한의 변경으로 가장 큰 효과를 내며, 필요가 입증되지 않은 기능과 계층을 추가하지 않는다.
- 프레임워크, 빌드 도구, 외부 라이브러리, 외부 폰트 및 CDN을 사용하지 않는다.
- 사용자 설정, 식별자 및 이용 기록을 쿠키나 웹 저장소에 보관하지 않는다.
- 오프라인 실행을 위해 앱 자체의 정적 파일만 브라우저 캐시에 저장한다.
- 실제 절전 방지 상태를 확인해 표시하며, 성공하지 않은 상태를 성공처럼 표현하지 않는다.
- 최신 주요 데스크톱 브라우저에서 동일한 핵심 사용 흐름을 제공하고, 미지원 환경에서는 명확한 안내를 제공한다.

## 3. 기능 범위

### 3.1 절전 방지

- 페이지가 로드되면 기간 제한 없이 Screen Wake Lock을 자동 요청한다.
- 자동 요청이 브라우저 정책(예: 사용자 조작 필요)에 의해 거부되면 이를 오류가 아니라 시작 안내 상태로 표시하고, 사용자가 한 번의 조작으로 시작할 수 있는 버튼을 둔다. 사용자의 조작에서 나온 요청이 거부된 때에만 오류 상태로 표시한다.
- 잠금이 운영체제나 브라우저에 의해 해제되면 즉시 화면 상태를 갱신한다.
- 페이지가 다시 보이는 상태가 되었을 때, 사용자가 중지하지 않았고 타이머가 끝나지 않았다면 잠금을 다시 요청한다.
- 사용자는 언제든 절전 방지를 직접 중지하거나 다시 시작할 수 있다.

### 3.2 타이머

- 기본값은 `제한 없음`이다.
- 제한 없음, 15분, 30분, 1시간, 2시간과 직접 입력을 드롭다운이 아닌 즉시 선택 버튼으로 제공한다.
- 직접 입력은 시간과 분을 각각 숫자로 받으며, 분은 0부터 59까지 허용한다.
- 타이머는 Wake Lock이 실제 활성화된 동안에만 흐르고, 중지되거나 일시 해제된 동안에는 멈춘다.
- 백그라운드 타이머 제한의 영향을 피하기 위해 활성 구간의 시작 시각과 종료 시각 차이를 누적한다.
- 시간이 끝나면 Wake Lock을 해제하고 상태, 남은 시간 및 문서 제목을 즉시 변경한다.

### 3.3 시간 표시

- Wake Lock이 실제 활성화된 누적 시간을 `시:분:초`로 표시한다.
- 사용자가 중지하거나 브라우저가 잠금을 해제한 동안에는 누적 시간이 증가하지 않는다.
- 제한 시간이 설정된 경우 남은 시간을 함께 표시한다.
- 사용자가 새 절전 방지 세션을 시작하거나 페이지를 다시 열면 누적 시간을 초기화한다.

### 3.4 언어

- 한국어와 영어를 지원한다.
- 언어 결정 우선순위는 URL의 `lang` 매개변수, 브라우저 언어, 한국어 기본값 순이다.
- 언어 전환 시 페이지를 새로고침하지 않고 URL의 `?lang=ko` 또는 `?lang=en`을 갱신한다.
- 언어 선택 자체는 쿠키나 웹 저장소에 기록하지 않는다.

### 3.5 오프라인 및 설치

- Service Worker로 앱 셸 파일만 캐시한다.
- 사용자 데이터나 실행 상태는 캐시에 넣지 않는다.
- Web App Manifest를 제공해 지원 브라우저에서 설치 가능한 형태로 만든다.
- 배포는 Cloudflare Pages와 `wakeup.subproject.kr`을 사용한다. 빌드나 서버 기능은 두지 않고, 보안 헤더는 `_headers`, 존재하지 않는 경로의 404 응답은 `_redirects` 한 줄로만 설정한다.

### 3.6 검색 기본 정보

- 서비스명은 `Wakeup`, 한 줄 설명은 한국어 `브라우저에서 화면이 잠들지 않게.`, 영어 `Keep your screen awake in the browser.`를 사용한다.
- 검색 기본 정보는 설명적인 문서 제목, 메타 설명, 배포 주소의 canonical URL과 의미 있는 단일 `h1`까지만 제공한다.
- 한 페이지 정적 도구의 현재 범위에서는 키워드 메타 태그, 구조화 데이터, 사이트맵과 별도 SEO 도구를 추가하지 않는다.

## 4. 화면 구성

단일 중앙 패널에 다음 요소를 위에서 아래 순서로 배치한다. 내부 영역은 반복적인 테두리와 구분선 대신 제목 크기, 글자 굵기, 색상과 충분한 여백으로 구분한다.

1. 서비스 이름, 한 줄 설명과 한국어/영어 전환 버튼
2. 현재 절전 방지 상태, 간단한 설명, 활성 누적 시간 및 필요 시 남은 시간
3. 시작 또는 중지 버튼
4. 타이머 즉시 선택 버튼과 필요 시 직접 입력 영역
5. 접힌 상태의 `작동 방식 및 제한 사항` 상세 설명
6. 프로젝트 저장소(GitHub)로 나가는 링크

제한 사항은 기본적으로 접어 두되, 표준 `details`/`summary` 요소로 키보드와 보조기기에서도 접근할 수 있게 한다.

6번처럼 다른 사이트로 나가는 링크는 자원을 불러오지 않으므로 외부 네트워크 의존성이 아니다. 새 탭으로 열고 `rel="noopener"`를 붙인다.

## 5. 상태 정의

- `requesting`: 절전 방지를 요청 중인 상태
- `prompt`: 사용자 조작 없는 자동 요청이 거부되어, 사용자가 시작 버튼을 눌러야 하는 상태
- `active`: Wake Lock이 실제 활성화된 상태
- `idle`: 사용자가 중지했거나 아직 시작되지 않은 상태
- `suspended`: 브라우저 또는 운영체제가 잠금을 해제한 상태
- `expired`: 설정한 타이머가 종료된 상태
- `unsupported`: 브라우저가 Screen Wake Lock API를 지원하지 않는 상태
- `error`: 요청이 실패한 상태

상태는 색상뿐 아니라 제목과 설명으로 구분하고, 변경 사항은 `aria-live` 영역으로 알린다.

## 6. 기술 및 저장소 구조

```text
/
├── .editorconfig
├── .gitattributes
├── .github/
│   └── workflows/
│       └── checks.yml
├── .gitignore
├── _headers
├── _redirects
├── index.html
├── styles.css
├── app.js
├── sw.js
├── manifest.webmanifest
├── favicon.svg
├── icons/
├── LICENSE
├── README.md
├── PLANNING.md
├── AGENTS.md
└── CLAUDE.md
```

- 번역 문구는 `app.js`의 단일 사전에서 관리한다.
- 시스템 기본 글꼴을 사용하고, 장식용 이미지나 아이콘 라이브러리는 두지 않는다.
- 캐시 이름에는 버전을 포함하고, 새 버전 활성화 시 이전 앱 캐시만 정리한다.
- 텍스트 파일은 UTF-8과 LF로 통일해 Windows와 macOS에서 같은 형식을 유지한다.
- 저장소에는 비밀정보, 개인 로컬 경로, 운영체제와 편집기의 로컬 파일을 포함하지 않는다.
- `_headers`에는 Cloudflare Pages에서 필요한 최소 보안 헤더와 Service Worker의 재검증 정책만 둔다.
- `_redirects`에는 존재하지 않는 경로를 `index.html` 본문과 `404` 상태로 응답하는 규칙 한 줄만 둔다.
- `.github/workflows/checks.yml`는 정적 검사(문법·manifest JSON·공백)만 실행한다. 빌드·배포 단계는 없고 실제 브라우저 검증은 사람이 한다.
- 배포물은 MIT 라이선스로 공개하고 `LICENSE`를 저장소 루트에 둔다.

## 7. 브라우저 제약

Screen Wake Lock은 보이는 활성 문서에서만 보장된다. 다른 탭으로 전환하거나 창을 최소화하면 해제될 수 있으며, 다시 돌아오면 자동으로 재요청한다. macOS에서는 창이 지금 보고 있지 않은 데스크톱(스페이스)에 있거나 다른 창에 완전히 가려져 있으면 `visibilityState`가 hidden이 되어 마찬가지로 해제된다. 다른 앱과 함께 사용할 때는 Wakeup을 별도 창으로 열어 지금 보는 데스크톱에 보이게 두고 최소화하지 않는 방법이 가장 안정적이다. 저전력 상태, 운영체제 정책과 닫힌 노트북 덮개는 웹페이지가 제어할 수 없다. 웹에서 제공되는 것은 화면 잠금이며 CPU만 별도로 깨워 두는 시스템 잠금은 아니다.

이 설명은 초기 화면을 방해하지 않도록 접힌 상세 영역에 제공한다. 잠금이 실제로 해제되거나 요청이 거부된 때에는 해당 사실을 주 상태 영역에도 표시한다.

## 8. 접근성 및 디자인 기준

- 본문과 조작 요소의 최소 글자 크기: 14pt
- 섹션 의미는 반복적인 상자와 구분선보다 타이포 위계와 여백으로 전달
- 충분한 명암비와 명확한 키보드 포커스 제공
- 상태를 색상에만 의존해 전달하지 않음
- 모든 입력 요소에 연결된 레이블 제공
- 의미 없는 애니메이션과 지속적인 움직임 사용 안 함
- 좁은 화면에서도 깨지지 않게 만들되 공식 지원 범위는 PC와 Mac으로 한정

## 9. 완료 기준

- 지원 브라우저에서 로드 직후 자동 요청이 실행된다.
- 자동 요청 실패 시 한 번의 사용자 동작으로 다시 시작할 수 있다.
- 시작, 중지, 강제 해제, 가시성 변경 및 타이머 종료 상태가 정확하게 표시된다.
- 활성 누적 시간과 제한 타이머는 Wake Lock이 실제 활성화된 동안에만 증가하거나 감소한다.
- 무제한 및 모든 제한 시간 선택이 정확하게 동작한다.
- 한국어와 영어 문구 및 URL 언어 상태가 일치한다.
- 서비스명, 한 줄 설명, 문서 제목, 메타 설명과 canonical URL이 서로 모순되지 않는다.
- 최초 온라인 방문 후 네트워크 없이 새로고침할 수 있다.
- 쿠키와 사용자 설정용 로컬 저장소를 만들지 않는다.
- 외부 네트워크 자원이나 런타임 의존성이 없다.
- 공개 저장소에 포함할 수 없는 비밀정보나 개인 로컬 경로가 없다.
- 문법 검사, 정적 파일 검사 및 실제 브라우저 핵심 흐름 검증을 통과한다.
