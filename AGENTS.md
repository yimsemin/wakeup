# AGENTS.md

## 프로젝트 목적

Wakeup은 데스크톱 브라우저에서 Screen Wake Lock을 이용해 화면 꺼짐과 잠금을 방지하는 작고 독립적인 정적 웹앱이다. 제품 요구사항과 확정된 동작은 `PLANNING.md`를 기준으로 한다.

## 변경 원칙

- 별도 지시가 없으면 한국어로 소통한다.
- 최소한의 변경으로 요청한 효과를 내는 것을 우선한다. 기존 파일과 구조로 해결할 수 있으면 새 파일, 계층, 추상화를 만들지 않는다.
- 핵심 기능의 정확성과 실제 상태 표시를 장식이나 부가 기능보다 우선한다.
- 순수 HTML, CSS, JavaScript만 사용한다.
- 프레임워크, 빌드 도구, 패키지 매니저, 외부 라이브러리, 외부 폰트 및 CDN을 추가하지 않는다.
- 요청하지 않은 기능, 호환성 우회책, 빌드·배포 자동화를 선제적으로 추가하지 않는다. SEO 관련 태그·파일은 `PLANNING.md` 3.6절에 정의된 범위로 한정하고 그 밖으로 확장하지 않는다. CI는 정적 검사(문법·JSON·공백·커밋 제목 형식) 워크플로 하나로 한정한다.
- 변경 전 관련 파일과 현재 Git 상태를 확인하고, 사용자 변경을 보존한다.
- 요청 범위 밖의 리팩터링이나 포맷 변경을 하지 않는다.
- 문서에는 현재 따라야 할 결정만 남기고 작업 일지나 폐기된 안을 누적하지 않는다. 같은 기준을 여러 문서에 중복하지 않는다.

## 기능 안전성

- `navigator.wakeLock.request("screen")`의 성공 여부와 `WakeLockSentinel`의 `release` 이벤트를 기준으로 상태를 표시한다.
- 실제 Wake Lock이 없을 때 `활성` 상태를 표시하지 않는다.
- 사용자가 중지했거나 타이머가 끝난 뒤에는 가시성 변경만으로 자동 재시작하지 않는다.
- 백그라운드 타이머 제한을 고려해 실제 Wake Lock 활성 구간의 시작·종료 시각 차이를 누적하고, 잠금이 없는 동안에는 활성 시간과 남은 시간을 진행하지 않는다.
- 지원하지 않는 브라우저에서 비디오 재생 같은 우회 기법을 사용하지 않는다.
- 타이머 종료 알림음은 Web Audio로 만든 짧은 단발음으로 제한한다. 외부 오디오 파일, 배경음, 지속 재생, 자리비움 방지용 무음 재생을 두지 않고, AudioContext는 사용자 조작에서 생성한다.
- ‘작은 창으로 열기’는 `window.open`과 같은 출처 `postMessage`(받는 쪽은 `event.origin` 확인)까지만 쓴다. 항상 위(Document PiP) 같은 별도 API나 라이브러리를 추가하지 않는다. 이 메시징은 저장·추적이 아니다.

## 데이터 및 오프라인

- 쿠키, `localStorage`, `sessionStorage`, IndexedDB에 사용자 설정이나 행동을 기록하지 않는다. 알림음 크기·테마 선택 같은 조절값도 세션 한정 메모리 값으로만 두고 저장하지 않는다.
- Service Worker Cache Storage에는 앱 자체의 정적 파일만 저장한다.
- 앱 셸 파일의 내용이나 목록을 변경하면 `sw.js`의 캐시 버전을 갱신한다. 작업 중 파일을 고칠 때마다 올리지 않고, 앱 셸 파일이 포함된 커밋을 만들 때 그 커밋에서 한 번만 올린다(한 커밋에서 +1).
- 분석, 광고, 원격 로깅 및 사용자 추적 코드를 추가하지 않는다.
- 앱 실행에 필요한 모든 파일은 같은 출처에서 제공한다.
- 다른 사이트로 나가는 링크(프로젝트 저장소, 문의 창구 등)는 허용한다. 링크는 자원을 로드하지 않으므로 외부 의존성이 아니다. 새 탭으로 열고 `rel="noopener"`를 붙인다. 외부 폼이나 위젯을 페이지에 삽입(iframe·스크립트)하는 것은 이 예외에 포함되지 않는다.

## UI 및 번역

- 글자 크기·접근성 기준은 `PLANNING.md` 8절을 따른다.
- 상태는 색상뿐 아니라 텍스트로 전달하고, 키보드 포커스와 보조기기 알림을 보존한다.
- 섹션 의미는 반복적인 테두리보다 타이포 위계와 여백으로 구분한다.
- 한국어와 영어 문구는 `app.js`의 번역 사전 한 곳에서 관리한다.
- 새 사용자 표시 문구를 추가할 때 두 언어를 동시에 추가한다. 영어 문구에서 "wake lock" 같은 개발 용어는 표준 기능 이름을 말할 때만 쓰고, 상태·시간 표시는 "keep the screen awake" 계열로 통일한다.
- 언어 URL 매개변수는 `lang=ko`와 `lang=en`만 허용한다.
- 서비스명은 `Wakeup`으로 유지하고 한 줄 설명은 `PLANNING.md`를 기준으로 한다.
- SEO 범위와 FAQ·구조화 데이터 문구 규칙은 `PLANNING.md` 3.6절이 기준이다.

## 저장소 및 배포

- 공개되어도 문제가 없도록 비밀정보, 개인 식별 정보, 개인 로컬 경로와 실제 환경 설정 파일을 커밋하지 않는다.
- 텍스트 파일은 UTF-8과 LF를 사용하고 `.gitattributes`와 `.editorconfig`를 기준으로 Windows와 macOS에서 같은 형식을 유지한다.
- 운영체제, 편집기와 에이전트가 만든 로컬 파일은 `.gitignore`에만 최소한으로 추가한다.
- 커밋 메시지와 PR 본문에 AI 생성 표식이나 도구 co-author 트레일러(`Co-authored-by: Claude …`, `🤖 Generated with …` 등)를 넣지 않는다. `git config core.hooksPath .githooks`로 활성화되는 `.githooks/commit-msg`가 모든 커밋 경로에서 해당 줄을 제거한다. 새 클론에서 한 번 실행해 둔다.
- 커밋 제목은 `type: 간결한 한글 설명` 형식으로 쓰고 마침표를 붙이지 않는다. type은 `feat`(사용자 기능 추가), `fix`(오류 수정), `docs`, `style`(동작 불변 UI·서식), `refactor`, `perf`, `test`, `build`, `ci`, `chore`, `revert`다. 형식은 `commit-msg` 훅이 검사하고, 훅을 우회한 커밋은 CI가 push 범위에서 다시 검사한다.
- push는 사용자가 그 시점에 명시적으로 승인한 경우에만 한다. 이전 승인은 이후 push로 이어지지 않는다. 기본 작업은 로컬 커밋까지다.
- 커밋은 작업 단위로 자유롭게 나누되, push 전에 원격에 없는 커밋(`origin/main..HEAD`)을 검토해 같은 목적의 커밋은 의미 단위로 squash하고, 정리한 목록을 사용자에게 보여 승인받은 뒤 push한다. 아직 push하지 않은 커밋만 다시 쓰며, 정리 전에는 백업 브랜치를 만든다.
- 이미 push된 원격 히스토리는 사용자의 명시적 승인 없이 force-push하거나 다시 쓰지 않는다.
- main에 push하면 Cloudflare Pages가 자동 배포한다. 릴리스는 git 태그(`vMAJOR.MINOR.PATCH`)로만 표시하며, 1.0 전에는 사용자에게 보이는 동작이 바뀌면 MINOR, 수정만이면 PATCH를 올린다. CHANGELOG 파일이나 코드 내 버전 문자열은 두지 않고, `sw.js`의 `CACHE_NAME`은 릴리스와 무관한 캐시 무효화 카운터다.

## 검증

- 변경 후 HTML, JavaScript, manifest 및 Service Worker의 기본 문법을 확인한다.
- `git diff --check`로 공백과 줄바꿈 오류를 확인하고, 공개 전에는 비밀정보와 개인 로컬 경로가 없는지 점검한다.
- `.github/workflows/checks.yml`이 푸시·PR마다 같은 검사(와 커밋 제목 형식)를 실행한다. 로컬에서도 먼저 한다. 구체적인 명령은 `CLAUDE.md`.
- 시작, 중지, 타이머 종료, 자동 해제, 재요청, 언어 전환 및 오프라인 흐름을 검증한다.
- 실제 운영체제의 절전 여부는 자동화만으로 단정하지 말고 Windows와 macOS의 지원 브라우저에서 수동 확인 항목으로 남긴다.
- 완료 보고에는 변경 내용, 검증 결과, 남은 브라우저 또는 운영체제 한계를 간결하게 적는다.
